                                  Git Commands

git clone "https://github.com/AbbuGunasekhar/DATA-STRUCTURES-AND-ALGORITHMS.git"

git status => Untracked (git add) =>Staging Area (git commit -m)=>Local Git (git push origin)

insert->add->commit->push

git add filename

git commit git commit -m "message"

git push origin main


               From GitHub to VS Code(Local Git)
for the first time when we want a rep:git clone "https://github.com/AbbuGunasekhar/DATA-STRUCTURES-AND-ALGORITHMS.git"
git pull



                           
                    Important Commands:
 git init :(Used to convert normal local folder to git)
 git remote add origin https://github.com/AbbuGunasekhar/DATA-STRUCTURES-AND-ALGORITHMS.git
 above commande is used when :error: failed to push some refs to 'https://github.com/AbbuGunasekhar/CSS.git'

Show branch: git branch
Change Branch:git branch -M main

 git pull origin main --allow-unrelated-histories